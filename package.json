{
  "name": "multi-replace",
  "displayName": "multi-replace",
  "description": "Replaces text and filenames in the selected catalog preserving cases.",
  "version": "0.2.0",
  "publisher": "justerest",
  "keywords": [
    "replace",
    "rename",
    "files",
    "filename",
    "angular",
    "component"
  ],
  "engines": {
    "vscode": "^1.30.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/justerest/multi-replace-vsix.git"
  },
  "categories": [
    "Extension Packs"
  ],
  "activationEvents": [
    "onCommand:extension.multiReplace"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "menus": {
      "explorer/context": [
        {
          "command": "extension.multiReplace",
          "group": "_multiReplace",
          "when": "explorerViewletVisible"
        }
      ]
    },
    "commands": [
      {
        "command": "extension.multiReplace",
        "title": "Multi replace"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test"
  },
  "dependencies": {
    "@justerest/multi-replace": "^2.1.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.5",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.9.4",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3333",
    "vscode": "^1.1.30"
  }
}
